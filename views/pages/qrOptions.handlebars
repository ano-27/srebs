<div class="qr-popup-container">
  <div class="qr-popup">
    {{!-- <h4>Product ID: {{product.id}}</h4> --}}
    <p> {{ product.name }} </p>
    <p> {{ product.id }} </p>
    <p>Price: Rs {{ product.price }} </p>
    <div class="qr-actions">
      <button onclick="handleChoice('checkout')">Checkout</button>
      <button onclick="handleChoice('add-to-cart')">Add to Cart</button>
    </div>
  </div>
</div>

<script>
  function handleChoice(action) {
    const productId = '{{product.product_id}}';
    if (action === 'checkout') {
      window.location.href = `/checkout/${productId}`;
    } else if (action === 'add-to-cart') {
      window.location.href = `/add-to-cart/${productId}`;
    }
  }
</script>

<style>
  .qr-popup-container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
  }

  .qr-popup {
    background: #fff;
    padding: 30px;
    border-radius: 12px;
    text-align: center;
    max-width: 400px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
  }

  .qr-popup h2 {
    margin-bottom: 10px;
  }

  .qr-actions button {
    margin: 10px;
    padding: 10px 20px;
    font-weight: bold;
    border: none;
    border-radius: 5px;
    cursor: pointer;
  }

  .qr-actions button:first-child {
    background-color: #374ba2;
    color: white;
  }

  .qr-actions button:last-child {
    background-color: #c7730b;
    color: white;
  }
</style>